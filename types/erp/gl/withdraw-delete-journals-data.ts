/** Defines the journal batch targeted by the withdraw-and-delete test. */
export interface WithdrawDeleteJournalsData {
  /** Exact test batch that is pending approval and safe to delete. */
  journalBatchName: string;
}
