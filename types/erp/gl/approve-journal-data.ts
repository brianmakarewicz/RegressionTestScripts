/** Defines the existing journal batch targeted by the approval test. */
export interface ApproveJournalData {
  /** Exact journal batch currently assigned to the configured approver. */
  journalBatchName: string;
}
