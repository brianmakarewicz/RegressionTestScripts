/** Defines the journal batch shared by approval initiation and approval. */
export interface JournalApprovalData {
  /** Exact saved batch that will be submitted and subsequently approved. */
  journalBatchName: string;
}
