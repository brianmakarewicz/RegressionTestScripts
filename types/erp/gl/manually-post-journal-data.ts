/** Defines the existing journal batch targeted by the manual-posting test. */
export interface ManuallyPostJournalData {
  /** Exact saved batch eligible for Post Batch. */
  journalBatchName: string;
}
